{
  "name": "the-system-tearn1",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "echo 'Create-React-App is needed to run this. Run: npx create-react-app .'",
    "build": "echo 'Create-React-App is needed to run this. Run: npx create-react-app .'",
    "test": "echo 'Create-React-App is needed to run this. Run: npx create-react-app .'",
    "eject": "echo 'Create-React-App is needed to run this. Run: npx create-react-app .'"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}